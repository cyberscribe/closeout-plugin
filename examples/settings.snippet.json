{
  "//": "Merge these keys into the consuming project's .claude/settings.json.",

  "//1": "Makes the plugin available to every teammate on clone, with no /plugin commands to run. Swap the source for {\"source\":\"directory\",\"path\":\".claude/plugins/closeout\"} if you vendor it as a git submodule instead.",
  "extraKnownMarketplaces": {
    "closeout-marketplace": {
      "source": { "source": "github", "repo": "cyberscribe/closeout-plugin" }
    }
  },
  "enabledPlugins": {
    "closeout@closeout-marketplace": true
  },

  "//2": "Lets the agent delete a draft after promoting it, without a prompt. `rm` everywhere else still prompts.",
  "permissions": {
    "allow": [
      "Bash(rm *closeout-drafts*)"
    ]
  },

  "//3": "Only needed if you run with the sandbox enabled. The draft directory is outside the default writable set, so without this the in-session agent can read a draft but never delete it. Takes effect in a NEWLY STARTED session.",
  "sandbox": {
    "filesystem": {
      "allowWrite": [
        "~/.claude/closeout-drafts"
      ]
    }
  },

  "//4": "All optional — the plugin auto-detects a docs directory if you omit these.",
  "env": {
    "CLOSEOUT_MODEL": "sonnet",
    "CLOSEOUT_DOC_DIR": "docs",
    "CLOSEOUT_DECISIONS_FILE": "docs/DECISIONS.md"
  }
}
